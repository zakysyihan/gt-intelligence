services:
  gt-intelligence:
    build: .
    container_name: gt-intelligence
    ports:
      - "8000:8000"   # Custom UI (FastAPI)
      - "8501:8501"   # Streamlit (backup)
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
