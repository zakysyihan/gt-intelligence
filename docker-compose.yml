services:
  gt-intelligence:
    build: .
    container_name: gt-intelligence
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
